{
  "name": "davi-icons",
  "private": true,
  "workspaces": [
    "apps/*",
    "package"
  ],
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "turbo run build",
    "icons:sync": "tsx scripts/build.ts",
    "package:build": "cd package && npm run build",
    "icons:sync:build": "npm run icons:sync && npm run package:build",
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter web dev",
    "publish:pkg": "npm run publish:patch",
    "publish:patch": "npm run icons:sync && npm --prefix package version patch --no-git-tag-version && npm publish ./package --access public",
    "publish:minor": "npm run icons:sync && npm --prefix package version minor --no-git-tag-version && npm publish ./package --access public"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@types/svg-parser": "^2.0.6",
    "fast-glob": "^3.3.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "svg-parser": "^2.1.0",
    "svgo": "^3.3.5",
    "tsx": "^4.19.2",
    "turbo": "^2.10.12",
    "typescript": "^5.8.3"
  }
}